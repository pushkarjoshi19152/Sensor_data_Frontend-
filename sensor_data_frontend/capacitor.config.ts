import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sensor_data_frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
